{
  "config": {
    "step": {
      "user": {
        "title": "Buspro gateway configuration",
        "description": "Enter IP address and port number of the gateway.",
        "data": {
          "host": "Host IP address",
          "port": "Port number",
          "send_port": "Send port (optional, defaults to port)",
          "receive_port": "Receive port (optional, defaults to port)"
        }
      }
    },
    "error": {
      "unknown": "An error occured."
    }
  }
}
